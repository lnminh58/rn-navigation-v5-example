import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 25,
    fontWeight: '700',
    color: '#f0932b',
    alignSelf: 'center',
    marginVertical: 30,
  }
});

export default styles;
